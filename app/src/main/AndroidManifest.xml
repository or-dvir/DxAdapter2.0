<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hotmail.or_dvir.dxlibraries">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".scrollandvisibility.ActivityScrollAndVisibility" />
        <activity android:name=".swipeable.ActivitySwipe" />
        <activity android:name=".stickyheader.ActivityStickyHeader" />
        <activity android:name=".selectable.ActivitySelect" />
        <activity android:name=".expandable.ActivityExpand" />
        <activity android:name=".draggable.ActivityDrag" />
        <activity android:name=".FeatureActivity" />
        <activity android:name=".clickable.ActivityClick" />
        <activity android:name=".ActivityMain">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>